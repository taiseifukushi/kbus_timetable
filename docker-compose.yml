version: '3'
services:
  web:
    build:
      dockerfile: docker/Dockerfile
    command: |
      bash -c "rm -f tmp/pids/server.pid && \
        bundle exec rails s -p 3001 -b '0.0.0.0'"
    stdin_open: true
    tty: true
    volumes:
      - .:/rails_app
    ports:
      - "3001:3001"
