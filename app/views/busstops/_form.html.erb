<%= form_with(model: @busstop, url: "/", method: :get) do |f| %>
<%# <%= form_with(model: @busstop, url: "/search", method: :get) do |f| %>
  <% if @busstop%>
    <%= f.label :title, "乗車" %>
    <%= f.select :get_on, @busstop&.map{|b| [b, b]}, { include_blank: "選択して下さい" } %>
    <%= f.label :title, "降車" %>
    <%= f.select :get_on, @busstop&.map{|b| [b, b]}, { include_blank: "選択して下さい" } %>
  <% else %>
    <% @busstop = [ "テスト1", "テスト2" ] %>
  <%# <% else @busstop = { name: "テスト1" }%>
    <%# <% binding.pry  %>
    <%# <%= f.label :title, "乗車", class: "h4" %>
    <%# <%= f.select :get_on, @busstop&.map{|b| [b, b]}, { include_blank: "選択して下さい" }, class: "form-select"%>
    <%# <%= f.label :title, "降車", class: "h4" %>
    <%# <%= f.select :get_on, @busstop&.map{|b| [b, b]}, { include_blank: "選択して下さい" }, class: "form-select" %>
    <%= f.label :title, "乗車" %>
    <%= f.select :get_on, @busstop&.map{|b| [b, b]}, { include_blank: "選択して下さい" } %>
    <%= f.label :title, "降車" %>
    <%= f.select :get_on, @busstop&.map{|b| [b, b]}, { include_blank: "選択して下さい" } %>
  <% end %>
  <%# <%= f.submit "Enter", class: "btn btn-lg btn-primary"%>
  <%= f.submit "Enter" %>
<% end %>
